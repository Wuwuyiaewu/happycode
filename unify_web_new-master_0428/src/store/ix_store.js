export const ix_baseInfo = require('./modules/baseInfo').default
export const ix_quote = require('./modules/quote').default
export const ix_user = require('./modules/user').default
export const ix_getters = require('./getters').default

export default {
    ix_baseInfo,
    ix_quote,
    ix_user,
    ix_getters
}
